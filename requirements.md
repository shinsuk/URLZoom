📍 Project: URLZoom

## 1. 개요
크롬 브라우저의 기본 동작(도메인 단위 줌 기억)을 우회하여, 특정 전체 URL(Full URL) 단위로 독립적인 확대/축소 비율을 고정하고 관리하는 확장 프로그램.

## 2. 핵심 요구사항 (Core Requirements)

### A. URL 기반 정밀 제어
- **Exact Match 우선**: 도메인(`tossinvest.com`)이 아닌 전체 경로(`.../stocks/A005930/order`)를 기준으로 줌 수치를 저장한다.
- **격리성**: 같은 도메인 내의 다른 URL(예: `/order` vs `/chart`)은 서로의 줌 설정에 영향을 주지 않아야 한다 (`chrome.tabs.setZoomSettings`의 `per-tab` scope 활용).

### B. 줌 상태 유지 및 복구 (Persistence)
- **Reload 대응**: 페이지 새로고침 시 크롬 기본값으로 튕기지 않고, 저장된 수치를 즉시 재주입한다.
- **Fallback 로직**: 줌 설정이 명시되지 않은 URL은 강제로 기본 줌으로 초기화되지 않으며, 크롬 본연의 도메인 단위 줌 기억 시스템(`per-origin`)을 온전히 유지하고 따른다.
- **Storage 사용**: 브라우저를 껐다 켜도 설정이 유지되도록 `chrome.storage`에 데이터를 보관한다.
- **웹 페이지 정보 저장**: 저장된 URL을 쉽게 구분할 수 있도록 웹 페이지의 제목(Title)도 함께 저장한다.

### C. 사용자 인터페이스 (UI/UX)
- **원클릭 저장 (Popup)**: 팝업 UI를 통해 현재 탭의 URL을 확인하고 즉시 수치를 입력하여 저장할 수 있어야 한다.
  - 저장된 설정이 없을 경우, 팝업 입력칸은 브라우저의 현재 줌 비율을 기본값으로 가져온다.
  - 현재 접속한 URL이 이미 고정된 수치가 있는지 팝업에서 즉시 확인이 가능하다.
- **상태 관리 (Options)**: "저장 목록 관리" 페이지를 제공하여 저장된 전체 목록을 한눈에 볼 수 있다.
  - **편집**: 저장된 제목과 URL, 줌 비율을 직접 텍스트 박스로 수정할 수 있다.
  - **정렬**: 각 열(제목, URL, Zoom)의 헤더를 클릭해 오름차순/내림차순으로 정렬(↑, ↓)할 수 있다.
- **설정 초기화**: 특정 URL에 대한 고정을 해제하고 기본값으로 되돌리는 '삭제' 기능을 제공한다.

## 3. 기능 상세 시나리오
| 단계 | 사용자 액션 | 시스템 반응 |
|---|---|---|
| 1 | 특정 URL 접속 (예: 토스증권 주문 페이지) | 브라우저 기본 줌(예: 125%)으로 로드됨 |
| 2 | 확장 프로그램 팝업 실행 | 현재 화면의 줌 비율(125%)이 기본으로 입력칸에 표시됨 |
| 3 | 80% 입력 및 저장 | 해당 탭의 줌이 즉시 80%로 변경되며 URL, 제목과 함께 저장됨 |
| 4 | 해당 페이지 새로고침(F5) | 로딩 시점에 저장된 데이터를 조회하여 80%를 강제 유지 |
| 5 | 동일 도메인의 다른 URL로 이동 | 저장된 값이 없으므로 강제 초기화하지 않고, 크롬 본연의 도메인 단위 줌 상태를 그대로 유지 (`per-origin`) |
| 6 | 옵션 페이지 진입 및 편집 | 목록을 정렬하거나, 기존 제목/URL/비율을 텍스트로 수정하여 저장 가능 |
| 7 | 설정 삭제 클릭 | 저장된 URL 매핑 데이터를 지우고 브라우저 기본 줌으로 즉시 복원 |

## 4. 기술적 고려사항
- **Event Trigger**: `chrome.tabs.onUpdated` 이벤트를 사용하여 `loading` 상태에서 줌을 주입함으로써 시각적 깜빡임을 최소화함.
- **Zoom Engine**: `chrome.tabs.setZoom` API를 활용하여 브라우저 내장 줌 엔진을 직접 제어함.
- **Zoom Scope**: 크롬 기본 `per-origin` 속성을 덮어씌워 특정 URL에서만 동작하도록 `per-tab` scope를 명시적으로 적용함.
- **URL Scope**: `location.href` 전체를 Key로 사용하여 쿼리 스트링이나 해시값 변화에도 대응할 수 있도록 설계함.
- **보안 예외 처리**: 브라우저 내부 페이지(`chrome://`, `edge://`, `about:`, `file://` 등)에서는 API 에러를 방지하기 위해 예외 처리 및 버튼 비활성화 조치를 함.

## 5. 제외 사항 (Out of Scope)
- 도메인 전체 일괄 적용 기능 (크롬 기본 기능과 중복되므로 제외).
- 정규표현식(Regex)을 이용한 복잡한 URL 매칭 (현재는 전체 URL 일치만 구현).
